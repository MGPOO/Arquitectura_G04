<Window x:Class="ClienteRestWpf.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Iniciar sesión"
        Height="600" Width="520"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#f3f7f9">

    <Window.Resources>

        <Style x:Key="GreenButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#178a58"/>
            <Setter Property="BorderBrush" Value="#178a58"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="46"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="InputBox" TargetType="TextBox">
            <Setter Property="Height" Value="44"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="10"/>
        </Style>

        <Style x:Key="PassBox" TargetType="PasswordBox">
            <Setter Property="Height" Value="44"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="10"/>
        </Style>

    </Window.Resources>

    <Grid>
        <Border Background="White" CornerRadius="22" Padding="36"
                HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="28" ShadowDepth="0" Opacity="0.20"/>
            </Border.Effect>

            <StackPanel>

                <TextBlock Text="Conversor Rest Dotnet G04"
                           FontSize="26" FontWeight="Bold"
                           Foreground="#0d6efd" TextAlignment="Center"/>

                <TextBlock Text="Escritorio" FontSize="16"
                           Foreground="#4a7bd6"
                           HorizontalAlignment="Center" Margin="0,2,0,16"/>

                <!-- REEMPLAZA ESTA IMAGEN CON TU ESCUDO -->
                <Image Source="/Views/Images/logo_liga.png"
                       Width="140" Height="140"
                       Stretch="Uniform"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10"/>

                <TextBlock Text="Iniciar Sesión"
                           FontSize="22"
                           FontWeight="Bold"
                           TextAlignment="Center" Margin="0,6,0,18"/>

                <TextBlock Text="Usuario" Margin="4,0,0,6"/>
                <TextBox x:Name="UserBox" Style="{StaticResource InputBox}"/>

                <TextBlock Text="Contraseña" Margin="4,14,0,6"/>
                <PasswordBox x:Name="PassBox" Style="{StaticResource PassBox}"/>

                <Button Content="Ingresar"
                        Style="{StaticResource GreenButton}"
                        Margin="0,20,0,0"
                        Click="OnLogin"/>

                <TextBlock x:Name="ErrorText"
                           Visibility="Collapsed"
                           Foreground="#c0392b"
                           Margin="0,14,0,0"
                           TextWrapping="Wrap"
                           FontSize="14"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
